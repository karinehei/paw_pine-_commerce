# Public site URL used for canonical links, Open Graph, and the sitemap.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional. When set, analytics events are also forwarded to a dataLayer
# compatible with Google Tag Manager / GA4.
NEXT_PUBLIC_GTM_ID=

# Optional. When true, analytics events are also printed to the browser console.
NEXT_PUBLIC_ANALYTICS_DEBUG=

# Shopify Storefront API (server only — never NEXT_PUBLIC_*).
# Headless → your storefront → private access token.
# Do not use an Admin API access token (shpat_ / shpca_). Do not wrap in quotes.
# When domain + token are set, the app uses the live shop.
# When they are absent, the app uses the local demo catalogue.
SHOPIFY_STORE_DOMAIN=your-store.myshopify.com
SHOPIFY_STOREFRONT_PRIVATE_TOKEN=
SHOPIFY_STOREFRONT_API_VERSION=2026-07

# Admin API for `npm run seed:shopify` only. Not used by the storefront.
# Dev Dashboard app (2026): Client ID + Client secret from App settings.
# The seed script exchanges these for a 24h token. Do not paste them into SHOPIFY_ADMIN_ACCESS_TOKEN.
SHOPIFY_CLIENT_ID=
SHOPIFY_CLIENT_SECRET=
# Optional legacy store custom-app token (shpat_…). Leave empty if using Client ID/secret.
SHOPIFY_ADMIN_ACCESS_TOKEN=
